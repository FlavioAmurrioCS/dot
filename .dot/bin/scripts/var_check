#!/usr/bin/env bash

function var_check() {
  local var_ndef=""
  for var in "$@"; do
    [ "${!var// /}x" = 'x' ] && var_ndef="${var_ndef} ${var}"
  done
  [ -n "${var_ndef}" ] &&
    msg --error "The following vars have not been defined:${var_ndef}" &&
    return 1
  return 0
}

var_check "${@}"
