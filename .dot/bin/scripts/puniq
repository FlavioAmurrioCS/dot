#!/usr/bin/env python

# Will take from stdin and on only pipe thru things it hasnt seen.

import sys

seen_words = set()

for line in sys.stdin:
    nline = line.strip()
    if nline not in seen_words:
        print(nline)
        seen_words.add(nline)
